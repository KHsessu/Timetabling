#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<time.h>



int make_random(void){

  /* 乱数系列の変更*/
  srand( (unsigned)time(NULL) );
}


int main(int argc, char* argv[]){

  
  char* readfile;
  char* writefile;
  int fNumOfStudent;
  int fNumOfProf;
  int fNumOfEvent;
  int fNumOfTimeInDay = 5;

  if ( argc != 3){
    printf( "Error: argc = %d\n",argc);
    return -1;
  }
  //引数を移す
  readfile = 

  //////////////////////////////
  fp = fopen(filename, "r")
  

}
